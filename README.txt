1. The obj folder within src/ is an empty folder for storing the object files generated by the makefile.
It is empty at the time of submission. (Do not delete.)
2. Makefile is within src/

Steps to generate executable:
1. cd src/
2. module load gcc-9.2
3. make clean
4. make

mmu executable then available in src/

